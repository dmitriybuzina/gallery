.body-images
  %h1
    All images
  .gallery
    -@images.each do |image|
      .item-image
        =link_to image_tag(image.file.url, class: 'img'), category_image_path(category_id: image.category.slug, id: image.id)
  =paginate @images