- @comments.each do |comment|
  %p
  -if comment.user
    .container-comment
      -if current_user.avatar.url.present?
        =image_tag(comment.user.avatar.url, class: 'avatar-comment')
      -else
        %span.glyphicon.glyphicon-user.avatar-comment
      =comment.user.email
      .comment-body
        =comment.body
        %br