.container-comments
  - @comments.each do |comment|
    %p
    -if comment.user
      .container-comment
        -#-if current_user.avatar.url.present?
        -#  =image_tag(comment.user.avatar.url, class: 'avatar-comment')
        -#-else
        -#  %span.avatar-comment.glyphicon.glyphicon-user
        .text-name-comment
          =comment.user.first_name + ' ' + comment.user.last_name
        .comment-body
          =comment.body
