.row-4
  - @images.each_slice(4) do |array_image|
    -array_image.each do |image|
      =link_to image_tag(image.file.url, size: "240x150"), category_image_path(@category, id: image.id)

%p= link_to image_tag('addImage.png', class: "img-thumbnail", size: "240x150"), "#form_modal", "data-toggle" => "modal"

= render 'images/modal'