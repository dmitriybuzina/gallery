.body-show
  =render 'images/form'
  .images
    - @images.each_slice(4) do |image|
      -image.each do |img|
        =link_to image_tag(img.file.url, class: 'img'), category_image_path(@category, id: img.id)
  = paginate @images

-#= render 'layouts/modal', modal_id: 'image_modal' do
-#  = render 'images/form'

#image-content